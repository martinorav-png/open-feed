import React from "react";
import ReactDOM from "react-dom/client";
import OpenFeed from "./OpenFeed.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <OpenFeed />
  </React.StrictMode>
);
