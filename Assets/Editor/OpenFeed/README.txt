OPENFEED — Editor scripts (filesystem) + menu bar
=================================================

All custom commands live under the Unity menu: OPEN FEED

  Store Flow/     Grocery store flow (generate, clear, targeted rebuild, night, GLB, trees…)
  Grocery Store/  Legacy grocery-scene generator (separate from Store Flow)
  Parking Lot/
  Driving/
  Desk/
  SixTwelve/
  UI/             Monitor & Phone
  Main Area/      One-off mesh helpers
  Project/        Build settings & empty scene creation
  Tools/          Export, camera path recorder, scene readout
  Textures/       PSX batch import

Folder layout here mirrors the menu where it helps (see subfolders).
